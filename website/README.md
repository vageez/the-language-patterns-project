# Website

The Astro Starlight application is configured at the repository root. Its canonical content is in `src/content/docs/`, UI customizations are in `src/components/` and `src/styles/`, and static assets are in `public/`.
